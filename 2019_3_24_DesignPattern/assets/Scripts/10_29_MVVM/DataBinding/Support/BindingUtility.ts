import { IDataContext } from "../DataContext/IDataContext";

export default class BindingUtility {
    public static findDataContext(current:cc.Node):IDataContext{
        let result:IDataContext=null;

        while(!result){
        }
    }
}
